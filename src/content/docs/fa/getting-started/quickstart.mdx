---
title: شروع سریع
description: راه‌اندازی بدرود و پیوستن به جلسه ویدیویی در کمتر از ۵ دقیقه
order: 1
---

بدرود را راه‌اندازی کنید و در کمتر از ۵ دقیقه به جلسه ویدیویی بپیوندید.

**مراحل این راهنما:** دانلود ← نصب ← تأیید ← پیوستن. برای سایر روش‌ها [راهنمای نصب](/fa/docs/getting-started/installation)، برای تنظیمات [پیکربندی](/fa/docs/getting-started/configuration) را ببینید.

> **یک باینری، بدون نیاز به اینترنت.** media server و SQLite داخلی دارد — در شبکه‌های ایزوله هم کار می‌کند.

---

## آنچه نیاز دارید

| نیاز | جزئیات |
|------|--------|
| سرور لینوکسی | amd64، دسترسی `sudo` و `systemd`. ویندوز: Docker یا WSL2. macOS: build از source |
| پورت‌های باز | `8090`، `7880`، `50000–60000/udp`. برای TLS خودکار: `80` و `443` هم باز باشد |
| ابزارها | `curl`، `tar` |
| دامنه (اختیاری) | رکورد DNS A به IP سرور. بدون دامنه؟ با IP هم کار می‌کند |

> **نحوه کار:** شما یک سرور **بدرود** اجرا می‌کنید.其他人 در **مرورگر** join می‌کنند (بدون نصب) یا از **اپلیکیشن بومی** (Android، Windows، macOS، Linux) استفاده می‌کنند.

---

## ۱. دانلود

```bash
curl -fsSL -o bedrud.tar.xz https://github.com/bedrud-ir/bedrud/releases/latest/download/bedrud_linux_amd64.tar.xz
tar -xJf bedrud.tar.xz
chmod +x bedrud
```

> **اینترنت ندارید؟** در هر ماشین‌ای دانلود کنید، با USB یا `scp bedrud.tar.xz user@server:/tmp/` منتقل کنید، سپس extract کنید. باینری کاملاً self-contained است.

> **دانلود شکست خورد؟** اتصال اینترنت را بررسی کنید، یا URL را در [GitHub Releases](https://github.com/bedrud-ir/bedrud/releases/latest) تأیید کنید.

---

## ۲. نصب

`meet.example.com` و `admin@example.com` را با مقادیر خود جایگزین کنید:

```bash
sudo ./bedrud install --tls --domain meet.example.com --email admin@example.com
```

باینری را نصب می‌کند، config تولید می‌کند، سرویس‌های systemd را ایجاد می‌کند، TLS را تنظیم و همه‌چیز را شروع می‌کند.

> **پورت ۸۰ یا ۴۴۳ اشغال است؟** سرویس‌های متضاد را متوقف کنید: `sudo systemctl stop nginx apache2 caddy`، سپس دوباره اجرا کنید.

> **دامنه ندارید؟** از IP سرور استفاده کنید:
> ```bash
> sudo ./bedrud install --tls --ip 1.2.3.4
> ```
> از self-signed certificate استفاده می‌شود. مرورگر هشدار می‌دهد اما ویدیو کار می‌کند. برای certificate معتبر در شبکه داخلی [راهنمای TLS داخلی](/fa/docs/guides/internal-tls) را ببینید.

---

## ۳. تأیید

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:8090
```

انتظار: `200`

> **۲۰۰ نیست؟** وضعیت سرویس: `sudo systemctl status bedrud livekit`. لاگ‌ها: `journalctl -u bedrud -u livekit --no-pager -n 50`.

---

## ۴. ثبت‌نام و ایجاد ادمین

`https://meet.example.com` (یا `https://<your-ip>:8090`) را در مرورگر خود باز کنید:

<CreateAdmin createOption />

۳. وارد شوید (یا رفرش کنید) - بخش **Admin** در نوار کناری ظاهر می‌شود (نمای کلی، کاربران، اتاق‌ها، تنظیمات). موفقیت: شما ادمین هستید.
۴. **ایجاد اتاق** ← **پیوستن** ← **اشتراک‌گذاری ویدیو/صدا** - هنگام درخواست، اجازه دوربین/میکروفون را بدهید

> **داشبورد ادمین** — پس از ارتقاء، به `/dashboard/admin` بروید. مدیریت کاربران، اتاق‌ها، تنظیمات سیستم و توکن‌های دعوت.

> **مرجع CLI** — مدیریت کامل کاربران: `bedrud user promote`، `demote`، `create`، `delete`.

**کلاینت‌های بومی** برای Android، Windows، macOS، و Linux: [GitHub Releases](https://github.com/bedrud-ir/bedrud/releases/latest). یا لینک جلسه را به اشتراک بگذارید - شرکت‌کنندگان در هر مرورگری می‌پیوندند.

---

## مسیرهای نصب جایگزین

### Docker

```bash
docker run -d --name bedrud \
  -p 8090:8090 \
  -p 7880:7880 \
  -p 50000-60000:50000-60000/udp \
  -v bedrud-data:/var/lib/bedrud \
  ghcr.io/bedrud-ir/bedrud:latest
```

تنظیمات سفارشی، Docker Compose، و متغیرهای محیطی: به [راهنمای Docker](/fa/docs/guides/docker) ببینید.

### ساخت از منبع

نیاز به [Go 1.24+](https://go.dev/dl/) و [Bun](https://bun.sh/) دارد:

```bash
git clone https://github.com/bedrud-ir/bedrud.git
cd bedrud
make init
make local-run
```

`make init` LiveKit را نصب می‌کند، تنظیمات را ایجاد می‌کند، وابستگی‌ها را دریافت می‌کند. `make local-run` می‌سازد و روی `http://localhost:8090` شروع می‌کند.

### HTTP ساده (فقط توسعه / localhost)

```bash
sudo ./bedrud install --ip 127.0.0.1
```

> **هشدار:** مرورگرها دوربین/میکروفون را روی غیر HTTPS بجز `localhost` مسدود می‌کنند. HTTP فقط برای توسعه است.

### سیستم‌های غیر لینوکسی

دستور `install` فقط برای لینوکس است. فایل باینری را به صورت دستی در PATH قرار دهید:

| سیستم‌عامل | نحوه انجام |
|-------------|-------------|
| macOS   | `sudo cp bedrud /usr/local/bin/`          |
| Windows | `bedrud.exe` را به پوشه‌ای در `%PATH%` منتقل کنید |

---

## شبکه‌های داخلی

بدون دامنه عمومی یا اینترنت خروجی؟ به [راهنمای TLS داخلی](/fa/docs/guides/internal-tls) برای تنظیم CA خصوصی و پیکربندی ذخیره‌سازی اعتماد کلاینت ببینید.

---

## پیکربندی

نصب‌کننده `/etc/bedrud/config.yaml` تولید می‌کند. پیش‌فرض‌ها برای اکثر موارد مناسب‌اند.
در تولید، `jwtSecret` و `sessionSecret` را تغییر دهید و restart کنید:

```bash
sudo systemctl restart bedrud livekit
```

برای تنظیم ظرفیت اتاق، زمان‌های انقضا، یا پورت‌های مدیا، به [بخش LiveKit در پیکربندی](/fa/docs/getting-started/configuration#livekit-configuration) ببینید.

مرجع کامل: [پیکربندی](/fa/docs/getting-started/configuration)

---

## عیب‌یابی

| مشکل | رفع |
|------|-----|
| پورت ۸۰/۴۴۳ در استفاده است | `sudo systemctl stop nginx apache2 caddy` |
| سرویس‌ها شروع نمی‌شوند | `journalctl -u bedrud -u livekit --no-pager -n 50` |
| خطاهای گواهی TLS | DNS را تأیید کنید: `dig meet.example.com`. باید به IP سرور اشاره کند |
| بدون ویدیو / WebRTC شکست خورد | برای دوربین HTTPS لازم است. از `--tls` استفاده کنید. UDP ۵۰۰۰۰–۶۰۰۰۰ را روی فایروال باز کنید |
| تداخل پورت LiveKit | از فلگ‌های `--lk-port`، `--lk-tcp-port`، `--lk-udp-port` استفاده کنید |
| فایروال مدیا را مسدود می‌کند | `sudo ufw allow 8090/tcp && sudo ufw allow 7880/tcp && sudo ufw allow 50000:60000/udp` |
| هشدارهای self-signed certificate | به [راهنمای TLS داخلی](/fa/docs/guides/internal-tls) برای تنظیم CA خصوصی ببینید |

---

## حذف

```bash
# نصب باینری
sudo ./bedrud uninstall

# Docker
docker stop bedrud && docker rm bedrud && docker volume rm bedrud-data

# ساخت از منبع
# برای توقف Ctrl+C، سپس: make clean
```

---

## مراحل بعدی

- [مرجع پیکربندی](/fa/docs/getting-started/configuration) - پورت‌ها، دیتابیس، providerهای احراز هویت، TURN
- [راهنمای نصب](/fa/docs/getting-started/installation) - package managerها، flagهای پیشرفته، تمام روش‌ها
- [راهنمای TLS داخلی](/fa/docs/guides/internal-tls) - استقرار offline، CA خصوصی
- [راهنمای استقرار](/fa/docs/guides/deployment) - تنظیمات production، reverse proxy، مقیاس‌پذیری
- [نمای کلی معماری](/fa/docs/architecture/overview) - نحوه اتصال اجزا
- [مستندات API](http://localhost:8090/api/swagger) - Swagger UI (نمونه در حال اجرا)
