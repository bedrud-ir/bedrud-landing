---
title: استقرار
description: استراتژی‌های استقرار بک‌اند
order: 21
---

نصب‌کننده داخلی یک سرور Linux تازه را با یک دستور برای استفاده در تولید پیکربندی می‌کند.

<InstallerSteps />

<SystemdServices />

## امنیت و TLS

نصب‌کننده امنیت را به صورت خودکار مدیریت می‌کند:

- **ACME (Let's Encrypt):** اگر یک دامنه و ایمیل ارائه دهید، تمدید خودکار گواهی را تنظیم می‌کند.
- **گواهی‌های امضاشده خود:** اگر دامنه‌ای ارائه نشده باشد، یک گواهی امضاشده خود تولید می‌کند تا سایت همچنان از طریق HTTPS کار کند.
- **پروکسی معکوس:** بک‌اند به عنوان پروکسی معکوس عمل می‌کند. ترافیک را در پورت ۴۴۳ دریافت می‌کند و درخواست‌های `/livekit` را به سرور رسانه داخلی می‌فرستد.

## حذف نصب

اگر نیاز به حذف Bedrud دارید، می‌توانید اجرا کنید:

```bash
sudo bedrud uninstall
```

این سرویس‌ها را متوقف می‌کند، فایل‌های systemd را حذف می‌کند، و پوشه‌های پیکربندی و داده را حذف می‌کند.

---

## مشاهده همچنین

- [راهنمای استقرار](/fa/docs/guides/deployment) - دستورالعمل‌های کامل استقرار در تولید
- [حالت لوازم](/fa/docs/guides/appliance) - مرجع تنظیم تک‌باینری
