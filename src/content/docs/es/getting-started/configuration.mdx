---
title: Configuración
description: Configura Bedrud según tus necesidades
order: 4
---

Bedrud utiliza archivos de configuración YAML tanto para el servidor principal como para el servidor de medios LiveKit integrado.

**Consulta también:** [Inicio Rápido](/es/docs/getting-started/quickstart) | [Instalación](/es/docs/getting-started/installation) | [Guía de Implementación](/es/docs/guides/deployment) | [Guía de Docker](/es/docs/guides/docker)

## Configuración Mínima de Producción

La configuración predeterminada funciona para desarrollo. Para producción, cambia estos valores en `/etc/bedrud/config.yaml`:

```yaml
auth:
  jwtSecret: "cambia-a-una-cadena-aleatoria-de-32-caracteres"
  sessionSecret: "cambia-a-otra-cadena-aleatoria"
```

Reinicia después de los cambios:

```bash
sudo systemctl restart bedrud livekit
```

Referencia completa a continuación.

---

## Configuración del Servidor

**Ubicación:** `server/config.yaml` (desarrollo) o `/etc/bedrud/config.yaml` (producción)

### Referencia Completa

```yaml
server:
  port: 8090                    # Puerto HTTP
  host: "localhost"             # Dirección de enlace

database:
  type: "sqlite"                # Tipo de base de datos: sqlite o postgres
  path: "data.db"               # Ruta del archivo de base de datos SQLite

logger:
  level: "debug"                # Nivel de registro: debug, info, warn, error
  outputPath: ""                # Ruta del archivo de registro (vacío = stdout)

livekit:
  host: "http://localhost:8090/livekit"  # URL externa de LiveKit
  internalHost: "http://127.0.0.1:7880"  # URL interna de LiveKit
  apiKey: "devkey"              # Clave API de LiveKit
  apiSecret: "devsecret"        # Secreto API de LiveKit

auth:
  jwtSecret: "tu-secreto-jwt"           # Secreto para firmar tokens JWT
  tokenDuration: 24                       # Expiración del token en horas
  sessionSecret: "tu-secreto-de-sesion"   # Secreto para cookies de sesión
  frontendURL: "http://localhost:8090"    # URL de interfaz (para redirecciones OAuth)

  # Proveedores OAuth (opcional)
  google:
    clientId: ""
    clientSecret: ""
  github:
    clientId: ""
    clientSecret: ""
  twitter:
    clientKey: ""
    clientSecret: ""

cors:
  allowedOrigins: "http://localhost:8090,http://localhost:3000"  # Separados por comas
  allowedHeaders: "Origin, Content-Type, Accept, Authorization"
  allowedMethods: "GET, POST, PUT, DELETE, OPTIONS"
  allowCredentials: true
```

### Configuraciones Clave

#### Base de Datos

De forma predeterminada, Bedrud usa **SQLite** con un archivo en la `path` configurada. Para producción con mayor concurrencia, cambia a PostgreSQL proporcionando una cadena de conexión en su lugar. Al usar PostgreSQL, el campo `path` contiene una cadena de conexión, no una ruta de archivo:

```yaml
database:
  type: "postgres"
  path: "postgres://usuario:contraseña@localhost:5432/bedrud?sslmode=disable"
```

#### Autenticación

El `jwtSecret` se usa para firmar tokens de acceso y actualización. Cámbialo del valor predeterminado en producción.

Los **proveedores OAuth** son opcionales. Si no los configuras, los botones de inicio de sesión social no aparecerán en la interfaz. Cada proveedor requiere registrar una aplicación OAuth con el servicio respectivo y proporcionar el ID de cliente y el secreto.

#### CORS

La cadena `allowedOrigins` (separada por comas) debe incluir la URL donde se sirve tu interfaz. En desarrollo, esto es `http://localhost:3000`. En producción, establécelo en tu dominio (por ejemplo, `https://meet.example.com`).

---

## Configuración de LiveKit

**Ubicación:** `server/config/livekit.yaml` (desarrollo) o `/etc/bedrud/livekit.yaml` (producción)

```yaml
port: 7880                      # Puerto HTTP/WebSocket de LiveKit

rtc:
  port_range_start: 50000       # Inicio del rango de puertos UDP
  port_range_end: 60000         # Fin del rango de puertos UDP
  use_external_ip: true         # Usar IP externa para RTC

turn:
  enabled: true
  domain: "localhost"
  tls_port: 5349
  udp_port: 3478

keys:
  devkey: "devsecret"           # Debe coincidir con la configuración del servidor

logging:
  level: info

room:
  auto_create: true              # Crear salas automáticamente cuando los participantes se unan
  empty_timeout: 60              # Segundos antes de eliminar sala vacía
  departure_timeout: 60          # Segundos para mantener la sala después de que todos los participantes se vayan
  max_participants: 20           # Máximo de participantes por sala (0 = ilimitado)
  enable_remote_unmute: true     # Permitir desactivación de silencio de participantes desde el servidor
```

<Callout type="warning">
    Las `keys` en `livekit.yaml` deben coincidir con `livekit.apiKey` y `livekit.apiSecret` en `config.yaml` del servidor.
</Callout>

### Rango de Puertos RTC

LiveKit usa puertos UDP para transmisiones de medios. El rango predeterminado `50000-60000` funciona para la mayoría de configuraciones. Si se ejecuta detrás de un firewall, asegúrate de que estos puertos estén abiertos.

Consulta [Conectividad WebRTC](/es/docs/architecture/webrtc-connectivity) para arquitectura y solución de problemas.

### Servidor TURN

El servidor TURN integrado retransmite medios para clientes detrás de NAT restrictivos o firewalls corporativos. Está habilitado de forma predeterminada en los puertos 3478 (UDP) y 5349 (TLS).

TURN es un retransmisor de último recurso: la mayoría de los clientes (~80%) se conectan directamente vía UDP y nunca lo usan. Cuando TURN se activa, el servidor transporta todo el ancho de banda de medios retransmitidos.

**Requisito de TLS:** TURN/TLS (puerto 5349) necesita un certificado TLS válido. Para producción, establece `turn.tls_port: 443` y apunta `cert_file`/`key_file` a tu certificado, o coloca un balanceador de carga de Capa 4 frente con `external_tls: true`.

Consulta la [Guía del Servidor TURN](/es/docs/architecture/turn-server) para arquitectura, detalles de configuración, cálculos de ancho de banda y solución de problemas.

### Configuraciones de Sala

La sección `room:` controla el comportamiento de las salas de reunión:

- **`auto_create`** - Crear salas automáticamente cuando los participantes se unan (predeterminado: `true`)
- **`empty_timeout`** - Segundos antes de eliminar una sala que nunca se unió (predeterminado: `60`)
- **`departure_timeout`** - Segundos para mantener la sala activa después de que todos los participantes se vayan (predeterminado: `60`)
- **`max_participants`** - Máximo de participantes por sala. Establece en `0` para sin límite (predeterminado: `20`)
- **`enable_remote_unmute`** - Permitir silenciar/desilenciar participantes desde el servidor (predeterminado: `true`)

**Ajuste para capacidad:**
- Reuniones de equipos pequeños: `max_participants: 10-20`
- Seminarios web grandes: `max_participants: 100` (o `0` para ilimitado)
- Servidores con recursos limitados: Reduce `max_participants` para disminuir el uso de CPU/memoria

---

## Variables de Entorno

Los valores de configuración se pueden anular con variables de entorno. La nomenclatura sigue una convención de prefijo por sección:

Para implementaciones con Docker, consulta la [Guía de Docker](/es/docs/guides/docker).

```bash
export SERVER_PORT=8090
export DB_PATH=/var/lib/bedrud/bedrud.db
export JWT_SECRET=production-secret
export LIVEKIT_HOST=http://localhost:8090/livekit
export LIVEKIT_API_KEY=prodkey
export LIVEKIT_API_SECRET=prodsecret
```

### Referencia Completa de Variables de Entorno

| Var de Entorno | Ruta YAML | Descripción |
|----------------|-----------|-------------|
| `SERVER_PORT` | `server.port` | Puerto de escucha HTTP |
| `SERVER_ENABLE_TLS` | `server.enableTLS` | Habilitar HTTPS (`true`/`false`) |
| `SERVER_CERT_FILE` | `server.certFile` | Ruta al certificado TLS |
| `SERVER_KEY_FILE` | `server.keyFile` | Ruta a la clave privada TLS |
| `SERVER_DOMAIN` | `server.domain` | Nombre de dominio |
| `SERVER_EMAIL` | `server.email` | Correo para Let's Encrypt |
| `SERVER_USE_ACME` | `server.useACME` | Habilitar Let's Encrypt automático (`true`/`false`) |
| `SERVER_TRUSTED_PROXIES` | `server.trustedProxies` | IPs de proxy de confianza separadas por comas |
| `SERVER_PROXY_HEADER` | `server.proxyHeader` | Encabezado para leer IP del cliente (por ejemplo, `X-Forwarded-For`) |
| `DB_HOST` | `database.host` | Host de base de datos (PostgreSQL) |
| `DB_PORT` | `database.port` | Puerto de base de datos |
| `DB_USER` | `database.user` | Usuario de base de datos |
| `DB_PASSWORD` | `database.password` | Contraseña de base de datos |
| `DB_NAME` | `database.dbname` | Nombre de base de datos |
| `DB_TYPE` | `database.type` | `sqlite` o `postgres` |
| `DB_PATH` | `database.path` | Ruta de archivo SQLite o cadena de conexión PostgreSQL |
| `LIVEKIT_HOST` | `livekit.host` | URL externa de LiveKit |
| `LIVEKIT_INTERNAL_HOST` | `livekit.internalHost` | URL interna de LiveKit |
| `LIVEKIT_API_KEY` | `livekit.apiKey` | Clave API de LiveKit |
| `LIVEKIT_API_SECRET` | `livekit.apiSecret` | Secreto API de LiveKit |
| `JWT_SECRET` | `auth.jwtSecret` | Secreto para firmar tokens JWT |
| `AUTH_FRONTEND_URL` | `auth.frontendURL` | URL de interfaz para redirecciones OAuth |
| `CORS_ALLOWED_ORIGINS` | `cors.allowedOrigins` | Orígenes permitidos separados por comas |
| `CORS_ALLOWED_HEADERS` | `cors.allowedHeaders` | Encabezados de solicitud permitidos |
| `CORS_ALLOWED_METHODS` | `cors.allowedMethods` | Métodos HTTP permitidos |
| `CORS_ALLOW_CREDENTIALS` | `cors.allowCredentials` | Permitir credenciales (`true`/`false`) |
| `CORS_EXPOSE_HEADERS` | `cors.exposeHeaders` | Encabezados expuestos al navegador |
| `CORS_MAX_AGE` | `cors.maxAge` | Duración de caché preflight en segundos |

---

## Lista de Verificación de Producción

- [ ] Cambia `jwtSecret` y `sessionSecret` a valores aleatorios fuertes
- [ ] Establece `logger.level` en `info` o `warn`
- [ ] Configura TLS (vía instalador o proxy inverso)
- [ ] Establece `cors.allowedOrigins` en tu dominio de producción
- [ ] Configura proveedores OAuth si es necesario
- [ ] Abre el rango de puertos RTC de LiveKit en tu firewall
- [ ] Configura rotación de registros para `/var/log/bedrud/`

Para una configuración completa de producción, consulta la [Guía de Implementación](/es/docs/guides/deployment).
