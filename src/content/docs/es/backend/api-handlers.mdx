---
title: Manejadores de API
description: Implementaciones de endpoints de la API REST
order: 19
---

Bedrud utiliza el framework web **Fiber** (API similar a Express para Go).

## Lógica de Enrutamiento

Las rutas se definen en `internal/server/server.go`. Agrupamos las rutas para aplicar middleware o prefijos fácilmente.

### Grupos Principales:

- **`/api/auth`**: Rutas públicas y protegidas para autenticación.
- **`/api/room`**: Rutas protegidas para la gestión de salas.
- **`/api/admin`**: Rutas restringidas a usuarios con el rol `superadmin`.
- **`/livekit`**: Un grupo proxy especial que envía solicitudes al servidor de audio/video LiveKit incrustado.

## Lógica del Manejador Especializado

### Gestión de Salas (`internal/handlers/room.go`)
El `RoomHandler` traduce entre los metadatos de la sala de Bedrud y el motor de medios de LiveKit.

#### 1. Creación de Sala
Cuando se crea una sala vía `POST /api/room/create`:

- **Normalización:** Los nombres de las salas se recortan y se convierten a minúsculas.
- **Generación Automática:** Si no se proporciona un nombre, Bedrud genera un nombre seguro para URL aleatorio (por ejemplo, `fancy-blue-whale`).
- **Creación Sincronizada:** El backend primero llama a la API interna de LiveKit para crear la sesión de medios, luego guarda los metadatos en la base de datos local.

#### 2. Unión y Tokens
Cuando un usuario se une vía `POST /api/room/join`:

- **Control de Acceso:** El backend verifica si la sala existe y si el usuario está autorizado.
- **Generación de Tokens:** Se genera un JWT firmado (Join Token) con:
  - `Identity`: El ID de Usuario de Bedrud.
  - `Name`: El Nombre para Mostrar del Usuario.
  - `Grants`: Permisos específicos como `CanJoin`, `CanPublish`, `CanSubscribe`.
- **Handshake del Cliente:** El frontend recibe tanto los metadatos locales de la sala como el token de LiveKit para iniciar la conexión WebRTC.

## Gestión de Administradores y Usuarios (`internal/handlers/users.go`)
Las rutas bajo `/api/admin` están estrictamente protegidas por el middleware `RequireAccess("superadmin")`.

### 1. Control de Usuarios
El `UsersHandler` permite a los administradores:

- **Listar Todos los Usuarios:** Obtener un directorio completo de usuarios registrados, incluyendo su proveedor de autenticación y metadatos del último inicio de sesión.
- **Actualizar Estado:** Activar o desactivar instantáneamente el acceso a la cuenta. Desactivar un usuario bloquea inmediatamente su capacidad de iniciar sesión o actualizar sus tokens.

### 2. Descripción General de Salas
Las rutas de administrador permiten listar todas las salas activas e históricas en toda la plataforma, independientemente de quién las creó. Esto está destinado a la moderación de la plataforma.

## Ciclo de Vida de la Solicitud

1.  **Solicitud recibida:** Fiber recibe la solicitud HTTP.
2.  **Middleware**:

    - `recover`: Evita que el servidor se bloquee si hay un error.
    - `cors`: Maneja el Intercambio de Recursos de Origen Cruzado (CORS).
    - `Protected`: (Opcional) Verifica un JWT válido en el encabezado `Authorization`.
3.  **Handler:** Se llama la función en `internal/handlers`.

    - Analiza el cuerpo JSON (si lo hay) usando `c.BodyParser`.
    - Llama al Servicio o Repositorio necesario.
    - Devuelve una respuesta JSON usando `c.JSON`.

## Ejemplo: Manejador de Creación de Sala

```go
func (h *RoomHandler) CreateRoom(c *fiber.Ctx) error {
    var input struct {
        Name string `json:"name"`
    }
    // 1. Analizar Entrada
    if err := c.BodyParser(&input); err != nil {
        return c.Status(400).JSON(fiber.Map{"error": "Invalid input"})
    }

    // 2. Lógica de Negocio (vía Repositorio)
    room := &models.Room{Name: input.Name}
    if err := h.roomRepo.Create(room); err != nil {
        return c.Status(500).JSON(fiber.Map{"error": "Failed to create room"})
    }

    // 3. Respuesta
    return c.Status(201).JSON(room)
}
```

## Archivos Estáticos (Frontend)

El frontend web está **incrustado** en el binario de Go en tiempo de compilación. Fiber sirve los archivos de React desde el directorio `frontend/` usando `filesystem.New`.

Cualquier ruta que no comience con `/api` se redirige al `index.html` de la aplicación React. Esto permite el enrutamiento del lado del cliente sin errores 404 al actualizar la página.
